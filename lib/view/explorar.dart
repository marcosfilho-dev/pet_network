import 'package:flutter/material.dart';

class Explorar_view extends StatefulWidget {
  const Explorar_view({super.key});

  @override
  State<Explorar_view> createState() => _Explorar_viewState();
}

class _Explorar_viewState extends State<Explorar_view> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(),
    );
  }
}